apiVersion: v1
kind: Pod
metadata:
    name: forgejo
    labels:
        app: forgejo
spec:
    restartPolicy: Always
    containers:
        - name: forgejo
          image: codeberg.org/forgejo/forgejo:14
          ports:
              - containerPort: 22
              - containerPort: 3000
          env:
              - name: USER_UID
                value: "1000"
              - name: USER_GID
                value: "1000"
              - name: TZ
                value: "Asia/Shanghai"
              - name: FORGEJO__server__ROOT_URL
                value: https://git.daveny.top/
              - name: FORGEJO__server__DOMAIN
                value: git.daveny.top
              - name: FORGEJO__server__SSH_DOMAIN
                value: git.daveny.top
          volumeMounts:
              - name: forgejo-data
                mountPath: /data
    volumes:
        - name: forgejo-data
          persistentVolumeClaim:
              claimName: forgejo-data
